***********)  Container 
=====================================

It is a widget that combines common painting , position and sizing of the child widgets 

It is also a class to store one or more widgets and position them on the screen according to our needs 

Generally it is similar to a box for storing contents

It allows many attributes to the user for decorating itself widget such as using margin which separates the container with other contents 

Container is a invisible box And we can store multiple things in it . 

Container has only one child , +but we can put multiple children's in it .